# global config
global:
  scrape_interval: 5s
  # evaluation_interval: 30s
  # body_size_limit: 15MB
  # sample_limit: 1500
  # target_limit: 30
  # label_limit: 30
  # label_name_length_limit: 200
  # label_value_length_limit: 200
  # scrape timeout is set to global default (10s)

  # TODO: Is this needed?
  # external_labels:
  #   monitor: codelab
  #   foo: bar

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets:
          - localhost:9090
  - job_name: "node"
    static_configs:
      - targets:
          - 10.255.0.11:9100
  - job_name: "disk"
    static_configs:
      - targets:
          - 10.255.0.11:9995
# TODO: Lower number -> higher cpu usage (default 75 @ v2.53)
# runtime:
#   gogc: 42

# TODO: Is this needed?
# rule_files:
#   - "first.rules"
#   - "my*.rules"

# TODO: Use to separate concerns
# scrape_config_files:

# TODO: Is this needed?
# alerting:
#   alert_relabel_config:
#   alertmanagers:
